import { BrowserRouter } from "react-router-dom";

import {} from "./components";

const App = () => {
  return (
    <BrowserRouter>
      <div className="relative z-0 bg-primary">test</div>
    </BrowserRouter>
  );
};

export default App;
